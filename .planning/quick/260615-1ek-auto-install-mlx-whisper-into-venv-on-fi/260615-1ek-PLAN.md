---
phase: quick-260615-1ek
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [transcribe.sh]
autonomous: true
requirements: [QUICK-1ek]

must_haves:
  truths:
    - "Running transcribe.sh on a machine with no .venv creates .venv and installs mlx-whisper without manual steps"
    - "Running transcribe.sh when .venv exists but mlx_whisper is not importable installs mlx-whisper into the existing .venv"
    - "Running transcribe.sh when mlx_whisper is already present skips install and proceeds to transcribe"
    - "transcribe.sh no longer prints the 'Please install it first' hard error and no longer exits 1 for a missing whisper binary"
  artifacts:
    - path: "transcribe.sh"
      provides: "setup_venv-style auto-install of mlx-whisper into shared .venv"
      contains: "VENV_DIR"
  key_links:
    - from: "transcribe.sh"
      to: ".venv/bin/mlx_whisper"
      via: "WHISPER_CMD set to venv binary after setup_venv"
      pattern: "WHISPER_CMD=.*VENV_DIR"
---

<objective>
Replace the hard "mlx_whisper not found" error in transcribe.sh (lines ~71-78) with a setup_venv-style auto-install step mirroring summarize-transcript.sh's idiom, so a first run is fully unattended and matches README claims (lines 30, 99).

Purpose: transcribe.sh currently dead-ends when mlx_whisper is absent, contradicting both the project's "unattended" core value and the sibling summarize-transcript.sh which auto-provisions the shared .venv. This closes that inconsistency.
Output: Modified transcribe.sh that creates/reuses .venv and installs mlx-whisper on demand, then points WHISPER_CMD at the venv binary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

@transcribe.sh
@summarize-transcript.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hard error with setup_venv-style auto-install of mlx-whisper</name>
  <files>transcribe.sh</files>
  <action>
In transcribe.sh, after the SCRIPT_DIR line (~69), define the same venv variables summarize-transcript.sh uses (lines 43-45): VENV_DIR="$SCRIPT_DIR/.venv", PYTHON="$VENV_DIR/bin/python", PIP="$VENV_DIR/bin/pip".

Replace the current block at lines ~71-78 (the WHISPER_CMD assignment plus the `if [ ! -f "$WHISPER_CMD" ]` hard-error-and-exit-1) with a setup_venv function and a call to it, modeled on summarize-transcript.sh setup_venv() (lines 49-65). The function MUST: (a) create the venv with `python3 -m venv "$VENV_DIR"` only if the directory is missing, printing a "Creating virtual environment" notice like the reference; (b) check importability with `if ! "$PYTHON" -c "import mlx_whisper" 2>/dev/null; then`, and inside that branch print an "Installing mlx-whisper" notice, run `"$PIP" install --upgrade pip > /dev/null`, then `"$PIP" install mlx-whisper`, then echo a success line. Mirror the reference's spacing/echo style but use mlx_whisper / mlx-whisper as the import name / pip package name.

After calling setup_venv, set WHISPER_CMD="$VENV_DIR/bin/mlx_whisper" so the rest of the script (the run invocation at line ~142) is unchanged. Do NOT keep the old "Please install it first" message or the `exit 1` for a missing binary.

Keep bash 3.2.57 compatibility (no mapfile, no `declare -A`, no float arithmetic in `(( ))`) — the added code is plain function + string assignment, so this is satisfied by avoiding any of those constructs. transcribe.sh has no `set -euo pipefail` at the top (unlike summarize-transcript.sh); do NOT add it — leave the script's existing error-handling/trap behavior untouched. Only change the venv/install region.
  </action>
  <verify>
    <automated>bash -n /Users/gareth/git/transcribrr/transcribe.sh && grep -q 'VENV_DIR="$SCRIPT_DIR/.venv"' /Users/gareth/git/transcribrr/transcribe.sh && grep -q 'pip install mlx-whisper' /Users/gareth/git/transcribrr/transcribe.sh && grep -q 'import mlx_whisper' /Users/gareth/git/transcribrr/transcribe.sh && grep -q 'WHISPER_CMD="$VENV_DIR/bin/mlx_whisper"' /Users/gareth/git/transcribrr/transcribe.sh && ! grep -q 'Please install it first' /Users/gareth/git/transcribrr/transcribe.sh</automated>
  </verify>
  <done>transcribe.sh passes `bash -n`; defines VENV_DIR/PYTHON/PIP; contains a setup_venv that creates .venv if absent and pip-installs mlx-whisper when `import mlx_whisper` fails; sets WHISPER_CMD to the venv binary after setup; and no longer contains the "Please install it first" hard error or its `exit 1`.</done>
</task>

</tasks>

<verification>
- `bash -n transcribe.sh` parses cleanly (no syntax errors, bash 3.2 stock interpreter).
- New code uses only constructs present in summarize-transcript.sh's setup_venv (function, if-guards, string assignment) — no mapfile, no `declare -A`, no float `(( ))`.
- The mlx-whisper install path targets the same .venv shared with summarize-transcript.sh (mlx-lm), which is the intended shared environment.
</verification>

<success_criteria>
- transcribe.sh auto-creates .venv and installs mlx-whisper on first run with no manual `pip install` step, matching README lines 30 and 99.
- Existing transcription flow (argument parsing, signal trap, progress monitor, output rename) is unchanged below the venv region.
- WHISPER_CMD resolves to "$SCRIPT_DIR/.venv/bin/mlx_whisper" after setup_venv.
</success_criteria>

<output>
Create `.planning/quick/260615-1ek-auto-install-mlx-whisper-into-venv-on-fi/260615-1ek-SUMMARY.md` when done.
</output>
