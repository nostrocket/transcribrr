---
phase: 04-benchmark-engine-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - transcribrr.sh
  - .gitignore
  - benchmark.sh
autonomous: true
requirements: [BENCH-01, BENCH-07]
must_haves:
  truths:
    - "Running transcribrr.sh --benchmark dispatches to benchmark.sh via exec (inherits the TTY)"
    - "benchmark.sh aborts cleanly with a clear message when stdin is not a TTY"
    - "benchmark.sh ensures the .venv (with mlx-lm + hf CLI) exists before any .venv/bin use"
    - "benchmark.sh parses config/candidates.conf (never sources it) and lists every candidate per stage including the last block"
    - "results/ is gitignored"
  artifacts:
    - path: "benchmark.sh"
      provides: "Benchmark engine skeleton: header, constants, TTY guard, setup_venv, ensure_dep, stage_banner, parse_candidates, SCRIPT_DIR paths"
      min_lines: 90
      contains: "parse_candidates"
    - path: "transcribrr.sh"
      provides: "--benchmark / --sample flag cases, exec-dispatch block, print_help update"
      contains: "BENCHMARK_MODE"
    - path: ".gitignore"
      provides: "results/ ignore entry"
      contains: "results/"
  key_links:
    - from: "transcribrr.sh"
      to: "benchmark.sh"
      via: "exec dispatch when BENCHMARK_MODE=true"
      pattern: "exec .*benchmark.sh"
    - from: "benchmark.sh"
      to: "config/candidates.conf"
      via: "parse_candidates (grep/while read, never source)"
      pattern: "parse_candidates"
---

<objective>
Build the dispatch path and the benchmark.sh foundation: `transcribrr.sh --benchmark` exec-dispatches (inheriting the TTY) to a new `benchmark.sh` that establishes its own environment — TTY guard, `setup_venv()` (which makes `.venv/bin/hf` and `.venv/bin/python` available, satisfying BENCH-07), copied helper functions (`ensure_dep`, `stage_banner`), the `candidates.conf` parser (parse-not-source), and `SCRIPT_DIR`-relative path constants. This is the wave-1 foundation every later plan extends.

Purpose: Without dispatch + skeleton, nothing else can run. The TTY guard (D-03) and `setup_venv`-first ordering (Pitfall B) are landmines that must be correct before any download/inference code is added.
Output: Modified `transcribrr.sh` (flag + dispatch + help), modified `.gitignore` (results/), and a new `benchmark.sh` with the skeleton and parser.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-benchmark-engine-core/04-CONTEXT.md
@.planning/phases/04-benchmark-engine-core/04-RESEARCH.md
@.planning/phases/04-benchmark-engine-core/04-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --benchmark / --sample dispatch to transcribrr.sh and ignore results/</name>
  <read_first>
    - transcribrr.sh (flag-parse loop lines 103-149; defaults block lines 12-29; settings.conf read ends line 185; print_help heredoc lines 38-99)
    - .gitignore (existing entries; the *_*/ pattern note at lines 19-26, config/settings.conf at line 28)
    - .planning/phases/04-benchmark-engine-core/04-PATTERNS.md (section "transcribrr.sh (MODIFIED)" — flag-parse extension, exec-dispatch block, print_help extension; section ".gitignore (MODIFIED)")
    - .planning/phases/04-benchmark-engine-core/04-CONTEXT.md (D-03, D-17)
  </read_first>
  <action>
    In transcribrr.sh: declare `BENCHMARK_MODE=false` and `BENCH_SAMPLE_ARG=""` in the defaults block (near lines 12-29). Add two `case` arms inside the existing flag-parse `while` loop, placed BEFORE the `-*)` catch-all: `--benchmark)` sets `BENCHMARK_MODE=true; shift`, and `--sample)` sets `BENCH_SAMPLE_ARG="$2"; shift 2`. After the settings.conf read section (after line 185) and BEFORE the URL/local input detection block, add the benchmark dispatch (D-17): when `BENCHMARK_MODE=true`, verify `$SCRIPT_DIR/benchmark.sh` exists and is executable (error + exit 1 otherwise), then `exec "$SCRIPT_DIR/benchmark.sh"` (or `exec "$SCRIPT_DIR/benchmark.sh" --sample "$BENCH_SAMPLE_ARG"` when `BENCH_SAMPLE_ARG` is non-empty). Use `exec` (not a forked subshell) so benchmark.sh inherits the terminal's TTY for the `[ -t 0 ]` guard. Update `print_help()` to document `--benchmark` (interactive TTY required; runs all hardware-fitting candidates per stage; prompts to pick best per stage; picks chain forward) and `--sample <url|mp3>` (override default sample, default https://www.youtube.com/watch?v=EWo7-azGHic). In .gitignore: add a `results/` entry after the `config/settings.conf` line with a brief comment noting per-run `*_*/` dirs are already caught but the parent must be explicit to suppress git-status noise.
  </action>
  <acceptance_criteria>
    - `grep -c 'BENCHMARK_MODE' transcribrr.sh` returns >= 3 (default + flag case + dispatch guard)
    - `grep -v '^#' transcribrr.sh | grep -c 'exec .*benchmark.sh'` returns >= 1
    - `--sample` and `--benchmark` both appear in the print_help heredoc: `grep -c -- '--benchmark' transcribrr.sh` >= 2 (case arm + help)
    - `grep -c '^results/$' .gitignore` returns 1
    - `bash -n transcribrr.sh` exits 0 (syntax valid)
    - The dispatch block appears after the settings.conf read (line number of the `exec` is greater than the line of the `_read_setting SUMMARY_MODEL_DEFAULT` block) and before the `IS_URL=true` URL-detection block
  </acceptance_criteria>
  <verify>
    <automated>bash -n /Users/gareth/git/transcribrr/transcribrr.sh && grep -q '^results/$' /Users/gareth/git/transcribrr/.gitignore && grep -q 'exec.*benchmark.sh' /Users/gareth/git/transcribrr/transcribrr.sh && EXEC_LN=$(grep -n 'exec.*benchmark.sh' /Users/gareth/git/transcribrr/transcribrr.sh | head -1 | cut -d: -f1) && SET_LN=$(grep -n 'SUMMARY_MODEL_DEFAULT' /Users/gareth/git/transcribrr/transcribrr.sh | head -1 | cut -d: -f1) && URL_LN=$(grep -n 'IS_URL=true' /Users/gareth/git/transcribrr/transcribrr.sh | head -1 | cut -d: -f1) && [ -n "$EXEC_LN" ] && [ -n "$SET_LN" ] && [ -n "$URL_LN" ] && [ "$EXEC_LN" -gt "$SET_LN" ] && [ "$EXEC_LN" -lt "$URL_LN" ]</automated>
  </verify>
  <done>transcribrr.sh parses --benchmark/--sample, exec-dispatches to benchmark.sh after settings read and before URL detection, documents both flags in help; .gitignore ignores results/.</done>
</task>

<task type="auto">
  <name>Task 2: Create benchmark.sh skeleton — TTY guard, setup_venv, helpers, candidates parser</name>
  <read_first>
    - transcribe.sh (setup_venv lines 76-92; SCRIPT_DIR/VENV_DIR/PYTHON/PIP lines 69-72; ffmpeg duration lines 103-113)
    - transcribrr.sh (header + SCRIPT_DIR lines 1-10; ERR trap lines 31-34; ensure_dep lines 223-252; stage_banner lines 301-308)
    - config/candidates.conf (the [candidate] block format: stage=/id=/label=/size_gb=; Qwen/ org entry at lines 66-70)
    - .planning/phases/04-benchmark-engine-core/04-PATTERNS.md (sections: Script header/SCRIPT_DIR; ERR trap + CURRENT_STAGE; setup_venv; TTY guard; candidates.conf parser; Hardware constants BENCH_OVERHEAD_BUFFER_GB/BENCH_COOLDOWN_SECS; ensure_dep verbatim; stage_banner verbatim)
    - .planning/phases/04-benchmark-engine-core/04-RESEARCH.md (Pattern 5 parser + Pitfall E emit-last-block; Pattern 6 TTY; Pitfall B setup_venv-first; Pitfall G label-vs-id)
  </read_first>
  <action>
    Create `benchmark.sh` at repo root, executable (`chmod +x`). Shebang `#!/bin/bash`, then `set -euo pipefail`, a header comment, and `SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"`. Define path constants from SCRIPT_DIR: `VENV_DIR`, `PYTHON="$VENV_DIR/bin/python"`, `PIP="$VENV_DIR/bin/pip"`, `HF_CLI="$VENV_DIR/bin/hf"`, `CANDIDATES_CONF="$SCRIPT_DIR/config/candidates.conf"`, `RESULTS_DIR="$SCRIPT_DIR/results"`, `HF_CACHE="${HOME}/.cache/huggingface/hub"`. Define tuning constants `BENCH_OVERHEAD_BUFFER_GB=4` and `BENCH_COOLDOWN_SECS=45` with comments citing D-07/D-14. Set `CURRENT_STAGE="init"` and an `ERR` trap that prints `Error: benchmark.sh failed during: $CURRENT_STAGE` to stderr — note in a comment that per-candidate failures will be bracketed with `set +e`/`set -e` (D-16, added in plan 04-03) so this trap does NOT abort the sweep on a single candidate failure. Parse `--sample` from "$@" into a `BENCH_SAMPLE_ARG` variable. Add the TTY guard FIRST (after arg parse): `if [ ! -t 0 ]; then` print the two-line "requires an interactive TTY" message to stderr and `exit 1` (D-03, use `[ -t 0 ]` on stdin per RESEARCH Pattern 6). Then copy `setup_venv()` verbatim from transcribe.sh BUT install `mlx-lm` (so `.venv/bin/hf` exists for pre-fetch) — call `setup_venv` immediately after the TTY guard (Pitfall B: before any `.venv/bin/*` access; satisfies BENCH-07). Copy `ensure_dep()` and `stage_banner()` verbatim from transcribrr.sh (exec-dispatch gives no access to the parent's functions). Add the `parse_candidates()` function from PATTERNS/RESEARCH Pattern 5 exactly — including the MANDATORY "emit last block" stanza after the `while read` loop (Pitfall E) so the last candidate per stage is not dropped. Do NOT `source` candidates.conf anywhere. End the file with a TEMPORARY smoke section that later plans replace: introduce it with a clearly marked guard comment `# TODO: REMOVE AFTER 04-04 — temporary skeleton smoke check` on the line immediately preceding the block, then (after setup) print the detected candidate counts per stage via `parse_candidates` (e.g. count whisper/cleanup/summarize lines) so the skeleton is runnable and testable now. Plan 04-04 Task 1 strips this whole guarded block when it wires the real staged sweep — the `# TODO: REMOVE AFTER 04-04` marker is the removal anchor.
  </action>
  <acceptance_criteria>
    - `bash -n benchmark.sh` exits 0
    - `test -x benchmark.sh` (file is executable)
    - `grep -c 'source ' benchmark.sh` returns 0 AND `grep -cE '^\s*\.\s' benchmark.sh` returns 0 (candidates.conf is never sourced)
    - benchmark.sh contains `parse_candidates`, `setup_venv`, `ensure_dep`, `stage_banner`, `[ ! -t 0 ]`, `BENCH_OVERHEAD_BUFFER_GB=4`, `BENCH_COOLDOWN_SECS=45`, `SCRIPT_DIR=`
    - The temporary smoke section carries its removal anchor: `grep -c 'TODO: REMOVE AFTER 04-04' benchmark.sh` returns 1 (04-04 Task 1 strips the block by this marker)
    - The "emit last block" stanza exists: `parse_candidates` body contains two `printf '%s|%s|%s\n'` occurrences (flush-on-next-block AND flush-after-loop)
    - No-TTY abort proves out: `bash benchmark.sh </dev/null; echo "exit=$?"` prints a message containing "interactive TTY" on stderr and `exit=1`
    - Parser correctness: running `parse_candidates whisper` over config/candidates.conf yields exactly 4 lines (including the last `distil-whisper-large-v3` block); `summarize` yields exactly 5 lines (including the last `Llama-3.3-70B` block)
  </acceptance_criteria>
  <verify>
    <automated>bash -n /Users/gareth/git/transcribrr/benchmark.sh && test -x /Users/gareth/git/transcribrr/benchmark.sh && [ "$(grep -c 'source ' /Users/gareth/git/transcribrr/benchmark.sh)" -eq 0 ] && [ "$(grep -c 'TODO: REMOVE AFTER 04-04' /Users/gareth/git/transcribrr/benchmark.sh)" -eq 1 ] && bash /Users/gareth/git/transcribrr/benchmark.sh </dev/null 2>&1 | grep -qi 'interactive TTY'</automated>
  </verify>
  <done>benchmark.sh exists, is executable, syntactically valid, guards on no-TTY, calls setup_venv before any .venv use, never sources config, parse_candidates returns all candidates per stage including the last block, and the temporary smoke section is marked with the `# TODO: REMOVE AFTER 04-04` removal anchor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config/candidates.conf → benchmark.sh | Untrusted config content (Phase 6 skill may write it) crosses into the engine; must be parsed, never executed |
| CLI args (--sample) → transcribrr.sh/benchmark.sh | User/caller-supplied value passed toward yt-dlp later |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | candidates.conf parse in benchmark.sh | mitigate | parse_candidates uses `while read`/case only — NEVER `source`/`.`; acceptance criterion asserts zero source/dot-include of any config |
| T-04-02 | Elevation of Privilege | exec dispatch to benchmark.sh | mitigate | Dispatch only `exec`s the fixed `$SCRIPT_DIR/benchmark.sh` path (verified existing+executable); no candidate/config value influences the executed path |
| T-04-03 | Tampering | --sample CLI value | accept | Value is only stored here; validated/used in plan 04-02 (yt-dlp). No shell expansion in this plan; always double-quoted |
| T-04-SC | Tampering | mlx-lm install via setup_venv | accept | mlx-lm from PyPI is an existing project dependency already used by stage scripts (audited in prior phases); no new/unaudited package introduced |
</threat_model>

<verification>
- `bash -n` passes on both transcribrr.sh and benchmark.sh
- exec dispatch sits after the settings.conf read and before URL detection (line-number ordering asserted)
- No-TTY invocation of benchmark.sh aborts with the TTY message and exit 1
- parse_candidates returns the correct candidate counts per stage (4 whisper, 4 cleanup, 5 summarize)
- The temporary smoke section is marked `# TODO: REMOVE AFTER 04-04` (removed by 04-04 Task 1)
- No `source`/`.` of any config file in benchmark.sh
</verification>

<success_criteria>
- `transcribrr.sh --benchmark` exec-dispatches to benchmark.sh (TTY inherited)
- benchmark.sh skeleton is runnable: TTY guard, setup_venv-first, helpers, parser all present and correct
- results/ gitignored
- Maps to: BENCH-01 (dispatch path), BENCH-07 (setup_venv installs mlx-lm / hf availability)
</success_criteria>

<output>
Create `.planning/phases/04-benchmark-engine-core/04-01-SUMMARY.md` when done
</output>
</output>
