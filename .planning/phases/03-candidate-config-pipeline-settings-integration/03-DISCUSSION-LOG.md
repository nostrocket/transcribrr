# Phase 3: Candidate Config & Pipeline Settings Integration - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-14
**Phase:** 3-Candidate Config & Pipeline Settings Integration
**Areas discussed:** candidates.conf format, settings.conf ship-vs-user-local, Invalid-model error (CFG-03), Precedence edge cases (CFG-02) + provenance

---

## candidates.conf format (MODEL-01)

| Option | Description | Selected |
|--------|-------------|----------|
| Flat TSV, one model per line | stage⇥hf_id⇥label⇥approx_size_gb; trivial cut -f parsing | |
| Per-stage header blocks | STAGE_CANDIDATES header + one line per model (ARCHITECTURE Pattern 4) | |
| KEY=value records | `[candidate]` block with stage/id/label/size_gb keys; explicit & self-documenting | ✓ |

**User's choice:** KEY=value records (`[candidate]` blocks).
**Notes:** Self-documenting wins over terseness. Still parsed (never sourced); bash 3.2 portable. File is committed (ships MODEL-02 list).

---

## settings.conf — ship or user-local?

| Option | Description | Selected |
|--------|-------------|----------|
| User-local, gitignored | Built-ins stay in script; settings.conf only generated by benchmark/hand | |
| Committed with shipped defaults | settings.conf tracked in repo, duplicates built-ins | |
| Gitignored + committed .example | Real file gitignored; documented settings.conf.example ships | ✓ |

**User's choice:** Gitignored + committed `.example`.
**Notes:** Built-in defaults remain in transcribrr.sh; example documents format. Keys this phase = the three `*_MODEL_DEFAULT` only. Missing settings.conf → silent fall-through to built-ins (no prompt).

---

## Invalid-model error (CFG-03)

| Option | Description | Selected |
|--------|-------------|----------|
| Catch & translate stage failure | Let stage load fail; translate into actionable message naming model + settings.conf source | ✓ |
| Cheap pre-flight, then catch | No-network pre-check of settings models (label/format/local cache) before stages | |
| Network existence check | Query HF API per run — rejected, breaks offline constraint | |

**User's choice:** Catch & translate stage failure.
**Notes:** No eager model load, fully offline. Accepted trade-off: cleanup/summary failures surface after transcription runs. Message points to --benchmark or explicit flag.

---

## Precedence edge cases (CFG-02) + provenance

| Option | Description | Selected |
|--------|-------------|----------|
| Per-flag sentinel | `*_EXPLICIT=true` set in each flag branch; settings applied only when false | ✓ |
| Compare against built-in default | Apply settings when var == hardcoded default — breaks `--whisper-model small` case | |

**User's choice:** Per-flag sentinel.
**Notes:** Correctly satisfies success criterion #3. Sentinel also yields clean provenance feeding the CFG-03 message.

| Option (Provenance) | Description | Selected |
|--------|-------------|----------|
| Yes, one-line summary | Print each model + its source (flag/settings.conf/built-in) before pipeline | ✓ |
| No, stay quiet | Only mention source in CFG-03 error | |

**User's choice:** Yes, one-line-per-stage provenance summary.

---

## Claude's Discretion

- Exact candidates.conf block-delimiter / parser implementation details.
- Precise wording and column formatting of the provenance and CFG-03 error lines (within the approved shapes).

## Deferred Ideas

- Cheap local-cache pre-flight validation for CFG-03 (rejected now; revisit if late-stage failures become painful).
- Network/HF existence validation of candidate IDs (Phase 6, SKILL-03).
- Extra settings.conf keys such as `CANDIDATE_MAX_AGE_DAYS` (Phases 5/6).
- MODEL-02 vetted-list *content* — left for the researcher to populate from current best mlx-community models.
