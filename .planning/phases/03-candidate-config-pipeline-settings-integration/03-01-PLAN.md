---
phase: 03-candidate-config-pipeline-settings-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/candidates.conf
  - config/settings.conf.example
  - .gitignore
autonomous: true
requirements: [MODEL-01, MODEL-02]
must_haves:
  truths:
    - "config/candidates.conf exists and lists real HF model IDs for all three stages (whisper, cleanup, summarize)"
    - "candidates.conf is a parse-not-source [candidate] KEY=value format containing no shell-executable syntax"
    - "config/settings.conf.example exists, is committed, and documents exactly the three model keys for this phase"
    - "config/settings.conf is gitignored so a per-user generated file is never committed"
  artifacts:
    - path: "config/candidates.conf"
      provides: "Vetted MODEL-02 candidate list in [candidate] KEY=value format (13 candidates: 4 whisper, 4 cleanup, 5 summarize)"
      contains: "[candidate]"
      min_lines: 60
    - path: "config/settings.conf.example"
      provides: "Committed template documenting WHISPER_MODEL_DEFAULT / CLEANUP_MODEL_DEFAULT / SUMMARY_MODEL_DEFAULT"
      contains: "WHISPER_MODEL_DEFAULT"
    - path: ".gitignore"
      provides: "Ignore rule for the per-user config/settings.conf"
      contains: "config/settings.conf"
  key_links:
    - from: "config/candidates.conf"
      to: "stage values whisper|cleanup|summarize"
      via: "stage= key per candidate block (D-02 stage vocabulary)"
      pattern: "stage=(whisper|cleanup|summarize)"
    - from: ".gitignore"
      to: "config/settings.conf"
      via: "ignore entry (D-04)"
      pattern: "config/settings\\.conf"
---

<objective>
Ship the committed config-data layer for v2.0: the vetted `config/candidates.conf` model list (MODEL-01/MODEL-02), the committed `config/settings.conf.example` template, and the `.gitignore` rule that keeps the per-user `config/settings.conf` out of version control.

Purpose: Phase 4's benchmark engine needs a real, parseable candidate list to sweep, and the normal pipeline (Plan 02) needs a documented settings format. This plan creates the data files; Plan 02 wires the reader. The two plans touch disjoint files and run in parallel.
Output: New `config/` directory containing `candidates.conf` and `settings.conf.example`; one new `.gitignore` entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-candidate-config-pipeline-settings-integration/03-CONTEXT.md
@.planning/phases/03-candidate-config-pipeline-settings-integration/03-RESEARCH.md
@.planning/phases/03-candidate-config-pipeline-settings-integration/03-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create config/ directory and write the vetted candidates.conf</name>
  <files>config/candidates.conf</files>
  <read_first>
    - .planning/phases/03-candidate-config-pipeline-settings-integration/03-RESEARCH.md — `## Code Examples` → "candidates.conf Shipped Format" contains the VERBATIM file body (13 candidate blocks with corrected HF IDs). Copy it exactly; do not re-derive IDs.
    - .planning/phases/03-candidate-config-pipeline-settings-integration/03-CONTEXT.md — D-01 (record format: [candidate] header + stage/id/label/size_gb keys), D-02 (parse-not-source; stage values whisper|cleanup|summarize), D-03 (committed file).
    - .planning/phases/03-candidate-config-pipeline-settings-integration/03-PATTERNS.md — "No Analog Found" note (config/ dir must be created first via mkdir -p) and RESEARCH Pitfall 5.
  </read_first>
  <action>
    First create the directory with `mkdir -p "$(git rev-parse --show-toplevel)/config"` (or repo-root config/) — RESEARCH Pitfall 5: the file write fails if config/ does not exist. Then write config/candidates.conf with the exact content from RESEARCH.md `## Code Examples` → "candidates.conf Shipped Format". The file MUST contain 13 [candidate] blocks: 4 whisper (small, turbo, turbo-4bit, distil-large-v3), 4 cleanup (llama3.2-1b-4bit, llama3.2-3b-4bit, llama3.1-8b-4bit, qwen3-8b-4bit), 5 summarize (Qwen2.5-14B-4bit, Qwen2.5-32B-4bit, Qwen3-14B-4bit, Qwen3-32B-4bit, Llama3.3-70B-4bit). Honor the RESEARCH corrections: the summarize Qwen3-14B entry uses id=Qwen/Qwen3-14B-MLX-4bit (Qwen org, NOT mlx-community, which does not exist as instruct); Llama3.3-70B-4bit carries size_gb=39.7. Each block is exactly: a `[candidate]` line, then `stage=`, `id=`, `label=`, `size_gb=` lines, with a blank line separating blocks (D-02 / Pitfall 3 — blank-line-terminated blocks). Keep the leading comment header. Use ONLY plain KEY=value text — no `$(...)`, no backticks, no `export`, nothing shell-evaluable (parse-not-source mitigation per D-02 and threat model T-03-02).
  </action>
  <verify>
    <automated>test -f config/candidates.conf && [ "$(grep -c '^\[candidate\]$' config/candidates.conf)" -eq 13 ] && [ "$(grep -cE '^stage=(whisper|cleanup|summarize)$' config/candidates.conf)" -eq 13 ] && grep -q '^id=Qwen/Qwen3-14B-MLX-4bit$' config/candidates.conf && ! grep -Eq '\$\(|`|^export |;[[:space:]]*rm' config/candidates.conf && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `grep -c '^\[candidate\]$' config/candidates.conf` returns exactly 13.
    - SOURCE: every `stage=` line matches one of whisper|cleanup|summarize (D-02 vocabulary); `grep -cE '^stage=(whisper|cleanup|summarize)$'` equals 13.
    - SOURCE: the summarize Qwen3-14B block uses `id=Qwen/Qwen3-14B-MLX-4bit` (RESEARCH correction — not mlx-community).
    - SECURITY (T-03-02 / D-02): no shell-evaluable tokens present — `grep -Eq '\$\(|`|^export |;[[:space:]]*rm'` finds nothing.
    - BEHAVIOR (ROADMAP success criterion #1): the file is data-only and parseable without sourcing; each candidate has stage, id, label, size_gb.
  </acceptance_criteria>
  <done>config/candidates.conf exists with 13 valid [candidate] blocks covering all three stages, real HF IDs (including the Qwen org Qwen3-14B correction), and contains no shell-executable syntax.</done>
</task>

<task type="auto">
  <name>Task 2: Write settings.conf.example template and add the .gitignore rule</name>
  <files>config/settings.conf.example, .gitignore</files>
  <read_first>
    - .planning/phases/03-candidate-config-pipeline-settings-integration/03-RESEARCH.md — `## Code Examples` → "settings.conf.example Shipped Format" (verbatim body) and ".gitignore Addition" (verbatim two-line entry).
    - .planning/phases/03-candidate-config-pipeline-settings-integration/03-CONTEXT.md — D-04 (settings.conf gitignored, settings.conf.example committed), D-06 (EXACTLY three keys this phase: WHISPER_MODEL_DEFAULT, CLEANUP_MODEL_DEFAULT, SUMMARY_MODEL_DEFAULT — no CANDIDATE_MAX_AGE_DAYS or other later-phase keys).
    - .gitignore (the file being modified) — append after the final comment block (current last content line is the "final *.md ... NOT gitignored" note); match the existing "comment line then path" style shown in 03-PATTERNS.md ".gitignore" section.
  </read_first>
  <action>
    Write config/settings.conf.example with the exact content from RESEARCH.md `## Code Examples` → "settings.conf.example Shipped Format": the explanatory comment header (precedence note, --benchmark pointer, value-format note) followed by exactly three keys — WHISPER_MODEL_DEFAULT=turbo, CLEANUP_MODEL_DEFAULT=llama3.1-8b-4bit, SUMMARY_MODEL_DEFAULT=Qwen2.5-32B-4bit. Do NOT add any fourth key (D-06; RESEARCH Anti-Pattern: no Phase 5/6 keys here). Then append to .gitignore the two-line entry from RESEARCH "## .gitignore Addition": a `# Per-user model selection (generated by --benchmark; do not commit)` comment line followed by `config/settings.conf` (repo-root-relative, no leading ./, matching the existing models/ and .venv/ style per 03-PATTERNS.md). The example file IS committed; only config/settings.conf (no .example) is ignored.
  </action>
  <verify>
    <automated>test -f config/settings.conf.example && [ "$(grep -vE '^#|^$' config/settings.conf.example | grep -cE '^(WHISPER|CLEANUP|SUMMARY)_MODEL_DEFAULT=')" -eq 3 ] && [ "$(grep -vE '^#|^$' config/settings.conf.example | grep -c '=')" -eq 3 ] && grep -qx 'config/settings.conf' .gitignore && ! grep -qx 'config/settings.conf.example' .gitignore && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE (D-06): settings.conf.example has EXACTLY three non-comment KEY= lines, all matching `^(WHISPER|CLEANUP|SUMMARY)_MODEL_DEFAULT=` — no fourth key.
    - SOURCE (D-04): `.gitignore` contains an exact line `config/settings.conf` AND does NOT ignore `config/settings.conf.example`.
    - BEHAVIOR: a fresh `git status` after this task shows config/settings.conf.example as a tracked/new file and would never show config/settings.conf if one were created.
  </acceptance_criteria>
  <done>config/settings.conf.example is committed with exactly the three Phase-3 model keys and a documenting header; .gitignore ignores config/settings.conf only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| machine/skill writer → candidates.conf | Phase 6 skill (or manual edit) writes candidates.conf; its content is untrusted when later parsed |
| user editor → settings.conf | A human (or errant script) edits the per-user settings.conf consumed in Plan 02 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Tampering | config/candidates.conf content | mitigate | File ships as plain `[candidate]` KEY=value data with NO shell-evaluable tokens; the parse-not-source contract (D-02) means a future writer cannot inject code that this file format would execute. Verify-gate asserts absence of `$(`, backticks, `export`, `; rm`. |
| T-03-04 | Information Disclosure | committed config files | accept | candidates.conf and settings.conf.example contain only public HF model IDs and labels — no secrets, no paths. Low risk. |
| T-03-05 | Tampering | accidental commit of per-user settings.conf | mitigate | `.gitignore` entry `config/settings.conf` prevents committing a user's machine-specific selections; the committed `.example` carries no user data. |

No package installs in this plan — package legitimacy gate (T-03-SC) is N/A for Phase 3 (RESEARCH `## Package Legitimacy Audit`).
</threat_model>

<verification>
- ROADMAP success criterion #1 (partial — file half): `config/candidates.conf` exists, lists real HF IDs for all three stages, and is plain data parseable without sourcing. (The "parsed by benchmark.sh" half lands in Phase 4; this plan delivers the file the parser will consume.)
- D-01/D-02/D-03 satisfied by Task 1; D-04/D-06 satisfied by Task 2.
- Run both task `<automated>` gates; both must print PASS.
</verification>

<success_criteria>
- config/candidates.conf: 13 [candidate] blocks, stage ∈ {whisper,cleanup,summarize}, real HF IDs (Qwen3-14B uses Qwen org), zero shell-evaluable syntax.
- config/settings.conf.example: exactly three model-default keys, committed.
- .gitignore: ignores config/settings.conf, not the .example.
</success_criteria>

<output>
Create `.planning/phases/03-candidate-config-pipeline-settings-integration/03-01-SUMMARY.md` when done.
</output>
