---
phase: 05-resumable-sweep-report-winner-selection
plan: 03
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - benchmark.sh
autonomous: false
requirements: [RESUME-01, RESUME-02, RPT-01, RPT-02, RPT-03, RPT-04, RPT-05]
must_haves:
  truths:
    - "On --benchmark, an incomplete most-recent run dir triggers `Resume interrupted run from <ts>? [Y/n]`; Yes reuses it, No mints a fresh run dir"
    - "On resume, already-completed model/stage pairs are skipped and recorded picks are reused without re-prompting; error JSONs are re-run"
    - "Immediately before the whisper winner prompt the divergence view renders to stderr (when >=2 readable transcripts)"
    - "Each new (non keep-current) stage pick is persisted to picks.json AND written atomically to config/settings.conf"
    - "After the sweep, a terminal ASCII table prints and report.md exists in the run dir"
  artifacts:
    - path: "benchmark.sh"
      provides: "wired resume + divergence view + per-stage persistence/settings write + report invocation"
      contains: "benchmark_helpers.py"
  key_links:
    - from: "RUN_DIR assignment (line ~872)"
      to: "detect_incomplete_run"
      via: "conditional resume vs fresh run dir + mkdir guard"
      pattern: "detect_incomplete_run"
    - from: "whisper select_best call (line ~1065)"
      to: "benchmark_helpers.py divergence"
      via: "DIVERG_ARGS from WHISPER_RESULTS_LIST, >&2"
      pattern: "benchmark_helpers.py divergence"
    - from: "each stage pick"
      to: "write_settings_key + persist_pick"
      via: "post-select_best, skipped when keep-current"
      pattern: "write_settings_key"
    - from: "after sweep_meta.json"
      to: "benchmark_helpers.py report"
      via: "report invocation writing report.md"
      pattern: "benchmark_helpers.py report"
---

<objective>
Wire the Wave-1 building blocks into the existing staged sweep so the full Phase 5 experience works end to end: resumable runs (RESUME-01/02), the pre-whisper divergence view (RPT-04/05), per-stage atomic winner persistence (RPT-02/03), and the post-sweep report (RPT-01). This is the integration plan — it touches only the call sites in `benchmark.sh`, relying on `benchmark_helpers.py` (05-01) and the new bash functions (05-02).

Purpose: keep the risky wiring (control flow, stdout discipline around select_best, resume branching) in one focused plan after both dependencies exist. Marked `autonomous: false` — the divergence view, keep-current menu, and resume prompt require a human-verify checkpoint on a real TTY (D-03 guarantees a TTY for --benchmark).

Output: `benchmark.sh` sweep is resumable, shows the divergence view before the whisper pick, persists winners atomically per stage, and emits the report.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-resumable-sweep-report-winner-selection/05-CONTEXT.md
@.planning/phases/05-resumable-sweep-report-winner-selection/05-RESEARCH.md
@.planning/phases/05-resumable-sweep-report-winner-selection/05-PATTERNS.md
@.planning/phases/05-resumable-sweep-report-winner-selection/05-01-SUMMARY.md
@.planning/phases/05-resumable-sweep-report-winner-selection/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resume wiring (RUN_DIR branch, skip loop, pick reuse) + current-default reads</name>
  <files>benchmark.sh</files>
  <read_first>
    - benchmark.sh lines 870-876 (the RUN_TS/RUN_DIR/mkdir block to wrap conditionally)
    - benchmark.sh lines 977-980 (SELECTED_TRANSCRIPT/CLEANED/SUMMARY init) — load_picks populates these on resume
    - benchmark.sh lines 1002-1063, 1076-1150, 1162-1235 (the three stage loops — insert should_skip_pair guards inside the per-candidate body)
    - benchmark.sh lines 1065, 1149, 1234 (the three select_best call sites)
    - 05-02-SUMMARY.md (exact signatures of detect_incomplete_run, should_skip_pair, persist_pick, load_picks, write_settings_key, select_best 3rd param + the `$RUN_DIR/.keep_current_<stage>` SENTINEL-FILE keep-current contract — select_best runs in a `$(...)` subshell so it signals keep-current via a file, not a variable)
    - transcribrr.sh lines 184-210 (_read_setting parse-not-source — copy to read current WHISPER/CLEANUP/SUMMARY defaults at sweep start)
    - .planning/phases/05-resumable-sweep-report-winner-selection/05-RESEARCH.md §"Pattern 4" (resume prompt + skip), Open Q2 (current default param), Open Q3 (re-run incomplete-stage candidates then select_best)
    - .planning/phases/05-resumable-sweep-report-winner-selection/05-CONTEXT.md D-11/D-13/D-14, §Deferred (guard resume against the run dir's own candidate set — note the changed-candidates.conf case)
  </read_first>
  <action>
    (1) Wrap the RUN_DIR block (line ~872): before minting RUN_TS, call `detect_incomplete_run`. If non-empty, derive `<ts>` and prompt `Resume interrupted run from <ts>? [Y/n] ` to stderr; on Y/y/empty set RUN_DIR/RUN_TS to that dir, `RESUMING=true`, and call `load_picks` (populates SELECTED_TRANSCRIPT/CLEANED/SUMMARY). On N set RESUMING=false. When NOT resuming, run the existing `RUN_TS=$(date …)`, RUN_DIR assignment, and `mkdir -p "$RUN_DIR/whisper" …` — the mkdir must NOT run when resuming (dirs already exist). Guard against the deferred changed-candidates.conf case by noting in the resume message that resume uses the existing run dir's results only.

    (2) Read current defaults at sweep start using a parse-not-source `_read_setting` helper (copy from transcribrr.sh:184-210, NEVER source): `CURRENT_WHISPER_DEFAULT`, `CURRENT_CLEANUP_DEFAULT`, `CURRENT_SUMMARY_DEFAULT` from `$SCRIPT_DIR/config/settings.conf`.

    (3) Inside each of the three stage loops, before running a candidate, call `should_skip_pair "$RUN_DIR/<stage>/<label>_result.json"`; if it returns 0 (skip), continue without re-running (the existing JSON is reused for the results list). Error JSONs (return 1) are re-run as today. On resume where a stage already has a recorded pick in picks.json (SELECTED_* already non-empty from load_picks), skip the entire select_best prompt for that stage and chain the recorded path forward (D-14, no re-prompt). For a partially-completed stage (candidates present, no recorded pick), re-run only the missing/error candidates then call select_best on the full accumulated list (Open Q3).
  </action>
  <verify>
    <automated>cd /Users/gareth/git/transcribrr && bash -n benchmark.sh && grep -q 'detect_incomplete_run' benchmark.sh && grep -q 'Resume interrupted run from' benchmark.sh && grep -q 'should_skip_pair' benchmark.sh && grep -q 'load_picks' benchmark.sh && grep -c 'should_skip_pair' benchmark.sh | { read n; [ "$n" -ge 3 ] && echo PASS_T1 || echo "FAIL only $n skip guards"; }</automated>
  </verify>
  <done>RUN_DIR conditionally reuses an incomplete run after a Y/n prompt; mkdir runs only on fresh runs; load_picks reuses recorded picks without re-prompting; should_skip_pair guards all three stage loops; current defaults read via parse-not-source. ROADMAP criterion #1 (restart skips completed pairs, continues) satisfied. `bash -n` clean; verify prints PASS_T1.</done>
</task>

<task type="auto">
  <name>Task 2: Divergence view + per-stage persistence/settings write + report invocation</name>
  <files>benchmark.sh</files>
  <read_first>
    - benchmark.sh line 1065 (whisper select_best call — divergence view goes immediately before; pass CURRENT_WHISPER_DEFAULT as 3rd arg)
    - benchmark.sh lines 1149, 1234 (cleanup/summarize select_best calls — pass current defaults; persist + settings write after)
    - benchmark.sh lines 1028-1061 (WHISPER_RESULTS_LIST build: `label|output_file|speed|peak` — source of DIVERG_ARGS; output_file is ABSOLUTE and OUTSIDE RUN_DIR, Pitfall 7)
    - benchmark.sh lines 1246-1263 (sweep_meta.json writer — report invocation goes immediately after)
    - 05-01-SUMMARY.md (benchmark_helpers.py divergence + report CLI contract, exit codes, stderr discipline)
    - 05-02-SUMMARY.md (persist_pick/write_settings_key signatures, the `$RUN_DIR/.keep_current_<stage>` SENTINEL-FILE keep-current contract, stage→key mapping)
    - benchmark.sh lines 779-786 (TIME_EXIT_FILE — the codebase's existing subshell-to-parent FILE-SIGNAL analog the keep-current sentinel follows: parent reads the file AFTER the `$(...)` subshell, then `rm -f`s it)
    - .planning/phases/05-resumable-sweep-report-winner-selection/05-RESEARCH.md §"Pattern 3" (invocation contract), §"Code Examples → divergence invocation", §"Pitfall 2" (stdout pollution), §"Pitfall 6" (picks vs settings SIGINT window)
    - .planning/phases/05-resumable-sweep-report-winner-selection/05-CONTEXT.md D-05/D-07/D-08/D-10
  </read_first>
  <action>
    (1) Divergence view (D-05, RPT-04/05): the whisper-stage statement ORDER must be exactly: (a) build `DIVERG_ARGS` → (b) call the divergence helper → (c) `SELECTED_TRANSCRIPT=$(select_best ...)` (line ~1065) → (d) `rm -f "$WHISPER_RESULTS_LIST"` (line ~1066, UNCHANGED position). The divergence block MUST read WHISPER_RESULTS_LIST BEFORE the existing `rm -f "$WHISPER_RESULTS_LIST"` deletes it — i.e. insert the DIVERG_ARGS build + helper call ABOVE the select_best call, never below the rm. Build `DIVERG_ARGS=()` by reading WHISPER_RESULTS_LIST (`IFS='|' read -r cand_label cand_output _rest`; `[ -f "$cand_output" ]` guard), appending `"${cand_label}:${cand_output}"`. Use the `${DIVERG_ARGS[@]+...}` set -u guard. If `${#DIVERG_ARGS[@]} -ge 2`, invoke `"$PYTHON" "$SCRIPT_DIR/benchmark_helpers.py" divergence --transcripts "${DIVERG_ARGS[@]}" --term-width "$(tput cols 2>/dev/null || echo 80)" >&2` inside a `set +e`/`set -e` bracket; on non-zero exit print a warning to stderr and CONTINUE (non-fatal). The explicit `>&2` is mandatory — any stdout from this call would corrupt the captured SELECTED_TRANSCRIPT (Pitfall 2).

    (2) Pass current defaults to each select_best: `select_best "whisper" "$WHISPER_RESULTS_LIST" "$CURRENT_WHISPER_DEFAULT"` (and cleanup/summarize equivalents). After EACH select_best returns a non-empty path, call `persist_pick "<stage>" "$SELECTED_*"`. Then determine keep-current via the SENTINEL FILE (NOT a module variable — select_best ran in the `$(...)` subshell so any variable it set is gone): `if [ -f "$RUN_DIR/.keep_current_<stage>" ]; then rm -f "$RUN_DIR/.keep_current_<stage>"; else write_settings_key <STAGE_KEY> "<winner_label>"; fi`. The `<stage>` token matches what select_best wrote (whisper/cleanup/summarize); ALWAYS `rm -f` the sentinel after reading it so it cannot leak into the next stage. The key is WHISPER_MODEL_DEFAULT / CLEANUP_MODEL_DEFAULT / SUMMARY_MODEL_DEFAULT (D-10) and winner_label is the label (field 1) of the selected list line — derive by grepping the results list (read it BEFORE its `rm -f`) for the selected output_file and taking field 1 (write the friendly label, not the absolute path or HF id, per D-10 + settings.conf.example). Keep-current (sentinel present) writes nothing to settings.conf (D-08), only persist_pick recorded the chained path.

    (3) Report (RPT-01): immediately after the sweep_meta.json writer (line ~1246-1263), invoke `"$PYTHON" "$SCRIPT_DIR/benchmark_helpers.py" report --run-dir "$RUN_DIR" --term-width "$(tput cols 2>/dev/null || echo 80)"` inside a `set +e`/`set -e` bracket; warn-and-continue on non-zero. This prints the terminal table to stderr and writes `$RUN_DIR/report.md`.

    Note Pitfall 6: the order per stage is select_best → persist_pick → write_settings_key. A SIGINT between persist_pick and write_settings_key is acceptable (settings.conf is whole-or-absent); document that on resume, if picks.json has a stage but settings.conf lacks the key, the pick is reused (chained) but the settings write is not retried this phase — note as a known minor gap.
  </action>
  <verify>
    <automated>cd /Users/gareth/git/transcribrr && bash -n benchmark.sh && grep -q 'benchmark_helpers.py divergence' benchmark.sh && grep -q 'benchmark_helpers.py report' benchmark.sh && grep -q 'persist_pick' benchmark.sh && grep -q 'write_settings_key' benchmark.sh && grep -q '.keep_current_' benchmark.sh && grep -B6 'select_best "whisper"' benchmark.sh | grep -q 'divergence' && echo PASS_T2</automated>
  </verify>
  <done>Statement order is DIVERG_ARGS build → divergence helper call → select_best → `rm -f "$WHISPER_RESULTS_LIST"` (divergence reads the list before it is deleted); divergence invoked >&2 immediately before the whisper pick (>=2 transcripts); each select_best gets the current-default 3rd arg; new picks persist to picks.json AND write the friendly label atomically to settings.conf; keep-current — detected via the `$RUN_DIR/.keep_current_<stage>` SENTINEL FILE that survives the `$(...)` subshell, then removed — writes nothing to settings.conf; report invoked after sweep_meta.json. ROADMAP criteria #2/#3/#4/#5 satisfied. `bash -n` clean; verify prints PASS_T2.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify the end-to-end resumable sweep on a real TTY</name>
  <what-built>
    The full Phase 5 sweep: resume prompt, pre-whisper divergence view (side-by-side columns at terminal width with per-model outlier counts), keep-current menu entry, atomic per-stage settings.conf writes, and the post-sweep terminal table + report.md.
  </what-built>
  <how-to-verify>
    1. Run `./transcribrr.sh --benchmark` (or `./benchmark.sh` directly) in a real terminal. Let the whisper stage finish ≥2 candidates.
    2. Confirm the divergence view appears BEFORE the whisper "Select the best" prompt — columns labeled per model, wrapped at your terminal width, with a per-model outlier count (or, with exactly 2 candidates, divergence counts and an explicit "no outlier ranking" note). No recommended winner is shown.
    3. Pick a winner. Confirm `config/settings.conf` now has the matching `WHISPER_MODEL_DEFAULT=<label>`. If the current default was among candidates, confirm a `[k] Keep current` entry was offered; picking it writes nothing.
    4. After the sweep, confirm a terminal ASCII results table prints and `results/benchmark_<ts>/report.md` exists with per-stage tables, excerpts, and a winners table.
    5. Re-run, Ctrl-C mid-whisper, then re-run again: confirm the `Resume interrupted run from <ts>? [Y/n]` prompt appears, Yes skips completed candidates and does not re-prompt for already-decided stages.
    6. (Criterion #6) During a settings write, confirm settings.conf is never left partial (it is either updated or unchanged).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g. divergence columns mis-wrapped, settings.conf not updated, resume re-ran completed pairs).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| transcript output_file content → divergence/report helper → terminal | Model-generated text rendered to the user's terminal (handled by 05-01 control-char stripping) |
| winner label → write_settings_key → settings.conf | Selected label persisted to config parsed by transcribrr.sh (validated in 05-02) |
| incomplete run dir → resume → chained inputs | A reused run dir's recorded picks feed downstream stages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08 | Tampering | select_best stdout pollution from divergence view | mitigate | Divergence invocation has explicit `>&2`; helper (05-01) writes only to stderr — SELECTED_TRANSCRIPT capture stays clean (Pitfall 2) |
| T-05-09 | DoS / Repudiation | helper subprocess failure mid-sweep | mitigate | divergence + report calls wrapped in set +e/set -e; non-zero exit warns and continues (continue-on-failure, sweep not aborted) |
| T-05-10 | Tampering | resume across a changed candidates.conf | accept | Deferred per CONTEXT; resume uses the existing run dir's own result set only; documented limitation, not reconciled this phase |
| T-05-SC | Tampering | package-manager installs | accept | No package installs this phase (stdlib only); no install surface |
</threat_model>

<verification>
- `bash -n benchmark.sh` clean after wiring.
- Divergence view invocation appears textually before the whisper select_best call and redirects to `>&2`.
- report invocation appears after the sweep_meta.json writer.
- write_settings_key is called only on new picks (when the `$RUN_DIR/.keep_current_<stage>` sentinel is ABSENT); keep-current (sentinel present) skips the settings write and removes the sentinel.
- Human-verify checkpoint approved on a real TTY.
</verification>

<success_criteria>
- ROADMAP criterion #1: Ctrl-C + restart resumes, skipping completed pairs.
- ROADMAP criteria #2/#3: report.md + terminal table both produced.
- ROADMAP criterion #4: divergence view before whisper prompt, full lines, labeled, outlier counts, never auto-picks.
- ROADMAP criterion #5: per-stage winner (or keep-current) written to settings.conf.
- All three task verifies pass (PASS_T1, PASS_T2) and the human-verify checkpoint is approved.
</success_criteria>

<output>
Create `.planning/phases/05-resumable-sweep-report-winner-selection/05-03-SUMMARY.md` when done.
</output>
